# ──────────────────────────────────────────────────────────────────
# SCD Rules Configuration
# Format: ID|RISK|PATTERN|DESCRIPTION
# Lines starting with '#' are comments. Blank lines are ignored.
# Use "regex:" prefix for POSIX extended regex patterns.
# ──────────────────────────────────────────────────────────────────

# R-01: Privilege escalation
R-01|HIGH|sudo su|Privilege escalation - sudo to root shell
R-01|HIGH|sudo -i|Privilege escalation - interactive root shell
R-01|HIGH|sudo bash|Privilege escalation - sudo to bash shell
R-01|HIGH|sudo sh|Privilege escalation - sudo to sh shell
R-01|HIGH|su - root|Switch user to root

# R-02: Recursive file deletion (CRITICAL)
R-02|CRITICAL|rm -rf /|Recursive deletion of root filesystem
R-02|CRITICAL|rm -rf /*|Recursive deletion of root contents
R-02|CRITICAL|rm -rf ~|Recursive deletion of home directory
R-02|CRITICAL|rm --no-preserve-root|Override rm root filesystem protection
R-02|CRITICAL|shred -u /|Secure file deletion of root filesystem

# R-03: Broad permission grants on system directories
R-03|HIGH|chmod 777 /etc|Broad permission grant on /etc
R-03|HIGH|chmod 777 /bin|Broad permission grant on /bin
R-03|HIGH|chmod 777 /usr|Broad permission grant on /usr
R-03|HIGH|chmod -R 777|Recursive world-writable permission grant
R-03|HIGH|chmod -R 0777|Recursive world-writable permission grant (octal)
R-03|HIGH|chmod 777 /|World-writable permission on root filesystem
R-03|HIGH|chmod a+s|Setuid/setgid bit set

# R-04: Remote code execution via pipe to shell (regex: require pipe to shell)
R-04|HIGH|regex:curl[[:space:]].*\|[[:space:]]*(ba)?sh|RCE: curl piped to shell
R-04|HIGH|regex:wget[[:space:]].*\|[[:space:]]*(ba)?sh|RCE: wget piped to shell
R-04|HIGH|regex:wget[[:space:]].*-O[[:space:]]*-|wget writing to stdout (pipe execution candidate)
R-04|HIGH|regex:curl[[:space:]].*-s[[:space:]].*\|[[:space:]]*(ba)?sh|RCE: silent curl piped to shell
R-04|HIGH|regex:fetch[[:space:]].*\|[[:space:]]*(ba)?sh|RCE: fetch piped to shell

# R-05: Reverse shell indicators (CRITICAL)
R-05|CRITICAL|nc -e|Reverse shell using netcat
R-05|CRITICAL|/dev/tcp|Reverse shell using /dev/tcp
R-05|CRITICAL|bash -i >& /dev|Interactive bash reverse shell
R-05|CRITICAL|ncat -e|Reverse shell using ncat
R-05|CRITICAL|socat exec|Reverse shell via socat exec
R-05|CRITICAL|regex:/dev/tcp/[0-9]|Direct /dev/tcp reverse shell
R-05|CRITICAL|python -c.*socket|Python socket-based reverse shell
R-05|CRITICAL|perl -e.*socket|Perl socket-based reverse shell

# R-06: Persistence mechanisms (require actual write to persistence paths)
R-06|MEDIUM|>> /etc/cron|Append to cron - persistence mechanism
R-06|MEDIUM|regex:>>[[:space:]]*/etc/cron|Write to cron files - persistence
R-06|MEDIUM|regex:>>[[:space:]]*~/\.bashrc|Append to .bashrc - persistence
R-06|MEDIUM|regex:>>[[:space:]]*~/\.bash_profile|Append to .bash_profile - persistence
R-06|MEDIUM|regex:>>[[:space:]]*~/\.profile|Append to .profile - persistence
R-06|MEDIUM|regex:>>[[:space:]]*~/\.zshrc|Append to .zshrc - persistence
R-06|MEDIUM|/etc/profile.d/|Writing to system-wide profile.d - persistence
R-06|MEDIUM|regex:crontab[[:space:]]+-l.*>|Overwriting crontab output - persistence
R-06|MEDIUM|crontab -|Writing to crontab via stdin - persistence
R-06|MEDIUM|at now|Scheduling command via at - persistence/delayed exec
R-06|MEDIUM|/etc/init.d/|Writing to init scripts - persistence
R-06|MEDIUM|~/.ssh/authorized_keys|Writing to authorized_keys - backdoor access
R-06|HIGH|regex:>>[[:space:]]*/etc/passwd|Append to /etc/passwd - account backdoor
R-06|HIGH|regex:>>[[:space:]]*/etc/sudoers|Append to sudoers - privilege persistence

# R-07: Obfuscated command execution
R-07|MEDIUM|base64 -d|Obfuscated command via base64 decode
R-07|MEDIUM|base64 -D|Obfuscated command via base64 decode (macOS)
R-07|HIGH|regex:base64[[:space:]].*\|[[:space:]]*(ba)?sh|Base64 decoded payload piped to shell
R-07|MEDIUM|xxd -r|Hex-encoded payload decode
R-07|MEDIUM|openssl enc -d|OpenSSL-decrypted payload
R-07|HIGH|regex:echo[[:space:]].*\|[[:space:]]*(ba)?sh|Encoded echo piped to shell
R-07|MEDIUM|eval $(|Eval of subshell output - obfuscation
R-07|MEDIUM|eval `|Eval of backtick subshell - obfuscation
R-07|MEDIUM|exec 3<|Indirect execution via file descriptor

# R-08: History evasion
R-08|LOW|unset HISTFILE|History file evasion
R-08|LOW|history -c|History clearing
R-08|LOW|HISTFILESIZE=0|History size evasion
R-08|LOW|HISTSIZE=0|History buffer evasion
R-08|LOW|set +o history|Disable bash history
R-08|LOW|HISTIGNORE=|Setting history ignore pattern

# R-09: Privileged file manipulation
R-09|HIGH|chown root|Privileged file ownership change
R-09|HIGH|chattr +i|Immutable file attribute set
R-09|HIGH|visudo|Direct sudoers file editing
R-09|HIGH|chmod 4755|SUID bit on executable
R-09|HIGH|chmod u+s|Setuid bit on file
R-09|MEDIUM|chattr -i|Remove immutable flag (possible before tampering)

# R-10: Aggressive process termination
R-10|MEDIUM|kill -9|Aggressive process termination
R-10|MEDIUM|pkill -f|Pattern-based process killing
R-10|HIGH|kill -9 -1|Kill all processes - system disruption
R-10|HIGH|killall -9|Kill all named processes

# ──────────────────────────────────────────────────────────────────
# REGEX RULES (use "regex:" prefix for POSIX extended regex)
# ──────────────────────────────────────────────────────────────────

# R-11: Script interpreter one-liners
R-11|HIGH|regex:(^|[[:space:]])python[23]?[[:space:]]+-c[[:space:]]|Python one-liner execution
R-11|HIGH|regex:(^|[[:space:]])perl[[:space:]]+-e[[:space:]]|Perl one-liner execution
R-11|HIGH|regex:(^|[[:space:]])ruby[[:space:]]+-e[[:space:]]|Ruby one-liner execution
R-11|HIGH|regex:(^|[[:space:]])node[[:space:]]+-e[[:space:]]|Node.js one-liner execution
R-11|HIGH|regex:(^|[[:space:]])php[[:space:]]+-r[[:space:]]|PHP one-liner execution

# R-12: Data exfiltration patterns (regex)
R-12|HIGH|regex:(^|[[:space:]])curl[[:space:]].*-d[[:space:]]|Data exfiltration via curl POST
R-12|HIGH|regex:(^|[[:space:]])scp[[:space:]].*@.*:|File transfer via scp
R-12|MEDIUM|regex:(^|[[:space:]])rsync[[:space:]].*@|Remote file sync (possible exfil)
R-12|MEDIUM|/dev/shm|Using shared memory filesystem (fileless technique)

# R-13: SSH-based attacks (regex)
R-13|MEDIUM|regex:(^|[[:space:]])ssh[[:space:]].*-R[[:space:]]|SSH reverse tunnel
R-13|MEDIUM|regex:(^|[[:space:]])ssh[[:space:]].*-L[[:space:]]|SSH local port forward
R-13|HIGH|regex:(^|[[:space:]])ssh[[:space:]].*-D[[:space:]]|SSH SOCKS proxy tunnel
R-13|HIGH|regex:ssh[[:space:]].*-o[[:space:]]*StrictHostKeyChecking=no|SSH strict host key disabled

# R-14: Container escape / privilege abuse
R-14|CRITICAL|docker run --privileged|Privileged container - full host access
R-14|CRITICAL|regex:docker[[:space:]].*--cap-add[[:space:]]|Docker capability escalation
R-14|CRITICAL|nsenter --target 1|Container namespace escape via nsenter
R-14|CRITICAL|regex:docker[[:space:]].*-v[[:space:]]*/:|Mount root filesystem into container
R-14|HIGH|regex:docker[[:space:]].*--net=host|Docker host network mode
R-14|HIGH|regex:docker[[:space:]].*--pid=host|Docker host PID namespace
R-14|HIGH|unshare --user|User namespace privilege escalation

# R-15: Credential and sensitive data access
R-15|CRITICAL|cat /etc/shadow|Reading shadow password file
R-15|HIGH|cat /etc/passwd|Reading system user database
R-15|HIGH|/proc/*/mem|Reading process memory (credential extraction)
R-15|HIGH|/proc/*/environ|Reading process environment (credential extraction)
R-15|CRITICAL|mimikatz|Credential dumping tool
R-15|HIGH|gsutil rsync|Google Cloud credential-bearing sync
R-15|MEDIUM|~/.aws/credentials|Access to AWS credentials file
R-15|MEDIUM|~/.ssh/id_rsa|Access to private SSH key
R-15|HIGH|regex:find[[:space:]].*-name[[:space:]].*password|Searching for password files
R-15|HIGH|regex:grep[[:space:]].*-r.*password[[:space:]]|Grepping for passwords recursively

# R-16: Fileless malware / memory execution techniques
R-16|CRITICAL|memfd_create|In-memory file execution (fileless malware)
R-16|CRITICAL|/proc/self/mem|Writing to own process memory
R-16|HIGH|dd if=/dev/mem|Reading raw memory device
R-16|HIGH|/dev/kmem|Access to kernel memory
R-16|HIGH|regex:ld\.so[[:space:]].*--preload|LD_PRELOAD injection via ld.so
R-16|HIGH|LD_PRELOAD=|Library injection via LD_PRELOAD
R-16|HIGH|LD_LIBRARY_PATH=/tmp|Suspicious library path pointing to /tmp

# R-17: Kernel and system tampering
R-17|CRITICAL|insmod|Loading kernel module (potential rootkit)
R-17|CRITICAL|modprobe|Modifying kernel modules
R-17|HIGH|sysctl -w|Writing kernel parameters
R-17|HIGH|regex:echo[[:space:]].*>[[:space:]]*/proc/sys|Writing to /proc/sys kernel parameters
R-17|HIGH|regex:echo[[:space:]].*>[[:space:]]*/sys/kernel|Writing to /sys/kernel
R-17|HIGH|dmesg -c|Clearing kernel message buffer
R-17|CRITICAL|kexec|Loading new kernel (possible rootkit install)

# ──────────────────────────────────────────────────────────────────
